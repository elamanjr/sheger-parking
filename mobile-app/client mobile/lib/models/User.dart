// ignore_for_file: file_names

class User {
  String fullName;
  String phone;
  String email;
  String passwordHash;
  String defaultPlateNumber;

  User(this.fullName, this.phone, this.email, this.passwordHash, this.defaultPlateNumber);
}